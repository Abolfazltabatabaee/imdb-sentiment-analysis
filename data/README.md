# Data Directory

This directory contains the dataset used in this project.

The project uses the IMDb Movie Reviews dataset for binary sentiment classification (positive / negative).

The raw dataset is not committed to the Git repository.
It is downloaded and extracted automatically when running the training script.

No manual setup is required.
